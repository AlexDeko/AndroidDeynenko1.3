<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.homework1_3.androiddeynenko">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/baseline_favorite_black_18dp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/baseline_favorite_black_18dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".VitalStatisticsActivity"></activity>
        <activity android:name=".PressureActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>